# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used by the daemon process.
# This is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m

# Specifies the JVM arguments used by the daemon process.
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m

# Runs gradle in parallel mode.
org.gradle.parallel=true

# AndroidX package structure
android.useAndroidX=true

# Enable AAPT2 PNG crunching
android.enablePngCrunchInReleaseBuilds=true

# Use this property to specify which architecture you want to build.
reactNativeArchitectures=armeabi-v7a,arm64-v8a,x86,x86_64

# Use this property to enable support to the new architecture.
newArchEnabled=true

# Use this property to enable or disable the Hermes JS engine.
hermesEnabled=true

# Enable edge-to-edge display support.
edgeToEdgeEnabled=true

# Enable GIF support in React Native images (~200 B increase)
expo.gif.enabled=true

# Enable webp support in React Native images (~85 KB increase)
expo.webp.enabled=true

# Enable animated webp support (~3.4 MB increase)
expo.webp.animated=false

# Enable network inspector
EX_DEV_CLIENT_NETWORK_INSPECTOR=true

# Use legacy packaging to compress native libraries in the resulting APK.
expo.useLegacyPackaging=false

expo.inlineModules.watchedDirectories=[]

# ---- RELEASE SIGNING (fill in these values for Play Store builds) ----
# Generate a keystore with:
#   keytool -genkeypair -v -keystore onehandindia-release.keystore \
#     -keyalg RSA -keysize 2048 -validity 10000 \
#     -alias onehandindia-release
# Then copy the keystore to android/app/ and fill in the values below.
MYAPP_RELEASE_STORE_FILE=onehandindia-release.keystore
MYAPP_RELEASE_KEY_ALIAS=onehandindia-release
MYAPP_RELEASE_STORE_PASSWORD=K769594s@#$
MYAPP_RELEASE_KEY_PASSWORD=K769594s@#$