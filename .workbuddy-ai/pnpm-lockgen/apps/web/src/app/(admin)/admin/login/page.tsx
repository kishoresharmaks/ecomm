import { AdminPortalShell } from "@/components/admin/admin-portal-shell";

export default function AdminLoginPage() {
  return (
    <AdminPortalShell
      title="Admin sign in"
      description="Use the standalone admin login before opening operational tools."
    >
      {null}
    </AdminPortalShell>
  );
}
